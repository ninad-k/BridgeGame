<UserControl x:Class="HonorBridge.Client.Wpf.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F0F0F0" Padding="50">
        <ScrollViewer>
            <StackPanel MaxWidth="800">
                <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                <TextBlock Text="{Binding RulesText}" FontSize="16" TextWrapping="Wrap"/>
                
                <Button Command="{Binding DataContext.NavigateHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                    Content="Back to Home" Margin="0,40,0,0" Padding="20,10" HorizontalAlignment="Left"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
