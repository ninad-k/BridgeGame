<UserControl x:Class="HonorBridge.Client.Wpf.Views.GameTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1024">
    <Grid Background="#0A420A"> <!-- Green Felt -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Top Bar / North -->
            <RowDefinition Height="*"/>    <!-- Play Area -->
            <RowDefinition Height="Auto"/> <!-- Hand / South -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/> <!-- West Info -->
            <ColumnDefinition Width="*"/>   <!-- Table -->
            <ColumnDefinition Width="200"/> <!-- East Info -->
        </Grid.ColumnDefinitions>

        <!-- Status Bar -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="#33000000" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding State.RoomId, StringFormat='Room: {0}'}" Foreground="White" Margin="0,0,20,0"/>
                <TextBlock Text="{Binding State.Phase, StringFormat='Phase: {0}'}" Foreground="White" Margin="0,0,20,0"/>
                <TextBlock Text="{Binding State.Declarer, StringFormat='Declarer: {0}'}" Foreground="White" Margin="0,0,20,0"/>
                 <TextBlock Text="{Binding State.Contract, StringFormat='Contract: {0}'}" Foreground="Yellow" FontWeight="Bold" Margin="0,0,20,0"/>
            </StackPanel>
        </Border>

        <!-- North Seat -->
        <Border Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" Margin="0,40,0,0">
            <StackPanel>
                <Button Command="{Binding SitCommand}" CommandParameter="North" Content="Sit North" Visibility="{Binding State.Seats[North], Converter={StaticResource NullToVis}}" />
                <TextBlock Text="{Binding State.Seats[North]}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                <!-- Dummy Hand (if visible) -->
                <ItemsControl ItemsSource="{Binding DummyHand}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="3" Margin="2" Width="40" Height="60">
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- West Seat -->
        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="20,0,0,0">
             <Button Command="{Binding SitCommand}" CommandParameter="West" Content="Sit West" Visibility="{Binding State.Seats[West], Converter={StaticResource NullToVis}}" />
             <TextBlock Text="{Binding State.Seats[West]}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <!-- East Seat -->
        <StackPanel Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,20,0">
             <Button Command="{Binding SitCommand}" CommandParameter="East" Content="Sit East" Visibility="{Binding State.Seats[East], Converter={StaticResource NullToVis}}" />
             <TextBlock Text="{Binding State.Seats[East]}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <!-- South Seat (Me) -->
        <Border Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center" Margin="0,0,0,20">
            <StackPanel>
                 <Button Command="{Binding SitCommand}" CommandParameter="South" Content="Sit South" Visibility="{Binding State.Seats[South], Converter={StaticResource NullToVis}}" />
                 <TextBlock Text="{Binding State.Seats[South]}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                 
                 <!-- My Hand -->
                 <ItemsControl ItemsSource="{Binding MyHand}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.PlayCardCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}"
                                    Width="50" Height="80" Margin="-10,0,0,0" Background="White">
                                <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="16"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Center Table (Trick) -->
        <Grid Grid.Row="1" Grid.Column="1">
            <TextBlock Text="N" Foreground="#88FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"/>
            <TextBlock Text="S" Foreground="#88FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
            <TextBlock Text="W" Foreground="#88FFFFFF" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBlock Text="E" Foreground="#88FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
            
            <!-- Cards played -->
            <Border Width="60" Height="90" Background="White" VerticalAlignment="Top" Margin="0,40,0,0" Visibility="{Binding State.CurrentTrick[North], Converter={StaticResource NullToVis}}">
                <TextBlock Text="{Binding State.CurrentTrick[North]}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
             <Border Width="60" Height="90" Background="White" VerticalAlignment="Bottom" Margin="0,0,0,40" Visibility="{Binding State.CurrentTrick[South], Converter={StaticResource NullToVis}}">
                <TextBlock Text="{Binding State.CurrentTrick[South]}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
             <Border Width="60" Height="90" Background="White" HorizontalAlignment="Left" Margin="40,0,0,0" Visibility="{Binding State.CurrentTrick[West], Converter={StaticResource NullToVis}}">
                <TextBlock Text="{Binding State.CurrentTrick[West]}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
             <Border Width="60" Height="90" Background="White" HorizontalAlignment="Right" Margin="0,0,40,0" Visibility="{Binding State.CurrentTrick[East], Converter={StaticResource NullToVis}}">
                <TextBlock Text="{Binding State.CurrentTrick[East]}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            
            <!-- Bidding Box Overlay (Visible during Bidding phase) -->
            <Border Background="#DD000000" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State.Phase}" Value="Bidding">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="Bidding" Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
                     <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding SelectedLevel}" Width="50"/>
                        <ComboBox ItemsSource="{Binding Strains}" SelectedItem="{Binding SelectedStrain}" Width="100" Margin="5,0,0,0"/>
                     </StackPanel>
                     <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                        <Button Command="{Binding BidCommand}" CommandParameter="Bid" Content="Bid" Width="60"/>
                        <Button Command="{Binding BidCommand}" CommandParameter="Pass" Content="Pass" Width="60" Margin="5,0,0,0"/>
                        <Button Command="{Binding BidCommand}" CommandParameter="Double" Content="X" Width="30" Margin="5,0,0,0" Foreground="Red" FontWeight="Bold"/>
                        <Button Command="{Binding BidCommand}" CommandParameter="Redouble" Content="XX" Width="30" Margin="5,0,0,0" Foreground="Blue" FontWeight="Bold"/>
                     </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Score Board -->
        <Border Grid.Row="2" Grid.Column="2" Background="#88000000" Margin="10" Padding="5">
            <StackPanel>
                <TextBlock Text="Tricks" Foreground="White" FontWeight="Bold"/>
                <TextBlock Text="{Binding State.TricksNS, StringFormat='NS: {0}'}" Foreground="White"/>
                <TextBlock Text="{Binding State.TricksEW, StringFormat='EW: {0}'}" Foreground="White"/>
                <TextBlock Text="{Binding State.LastScore}" Foreground="Yellow" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
