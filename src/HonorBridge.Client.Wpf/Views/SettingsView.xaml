<UserControl x:Class="HonorBridge.Client.Wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F0F0F0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="Settings" FontSize="32" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>
            
            <TextBlock Text="AI Bidding System" FontSize="18" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding BiddingSystems}" SelectedItem="{Binding SelectedSystem}" 
                      FontSize="16" Padding="10"/>
            <TextBlock Text="Select the system the AI will use for both Partner and Opponents." 
                       FontSize="12" Foreground="Gray" Margin="0,5,0,20" TextWrapping="Wrap"/>
            
            <Button Command="{Binding DataContext.NavigateHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                    Content="Save &amp; Return" Margin="0,20,0,0" Padding="10"/>
        </StackPanel>
    </Grid>
</UserControl>
