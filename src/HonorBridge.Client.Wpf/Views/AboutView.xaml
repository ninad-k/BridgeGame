<UserControl x:Class="HonorBridge.Client.Wpf.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F0F0F0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding AppName}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Version}" FontSize="16" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            
            <TextBlock Text="Created by:" HorizontalAlignment="Center" Margin="0,10,0,5"/>
            <TextBlock Text="{Binding Developer}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#005500"/>
            
            <TextBlock Text="Roles:" HorizontalAlignment="Center" Margin="0,20,0,5"/>
            <TextBlock Text="{Binding Roles}" FontSize="16" HorizontalAlignment="Center"/>
            
            <TextBlock Text="{Binding Dedication}" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,40,0,0"/>

            <Button Command="{Binding DataContext.NavigateHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                    Content="Back to Home" Margin="0,40,0,0" Padding="20,10"/>
        </StackPanel>
    </Grid>
</UserControl>
