<UserControl x:Class="HonorBridge.Client.Wpf.Views.HowToPlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F0F0F0" Padding="30">
        <ScrollViewer>
            <StackPanel MaxWidth="800">
                <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="Bold" Margin="0,0,0,20" Foreground="#004400"/>

                <!-- Basics -->
                <TextBlock Text="{Binding BasicsTitle}" FontSize="24" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBlock Text="{Binding BasicsContent}" FontSize="16" TextWrapping="Wrap" Margin="10,0,0,10"/>

                <!-- Auction -->
                <TextBlock Text="{Binding AuctionTitle}" FontSize="24" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBlock Text="{Binding AuctionContent}" FontSize="16" TextWrapping="Wrap" Margin="10,0,0,10"/>

                <!-- Play -->
                <TextBlock Text="{Binding PlayTitle}" FontSize="24" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBlock Text="{Binding PlayContent}" FontSize="16" TextWrapping="Wrap" Margin="10,0,0,10"/>

                <Separator Margin="0,20"/>

                <!-- Systems -->
                <TextBlock Text="{Binding SystemsTitle}" FontSize="28" FontWeight="Bold" Margin="0,10,0,5" Foreground="#004400"/>
                <TextBlock Text="{Binding SystemsIntro}" FontSize="16" TextWrapping="Wrap" Margin="10,0,0,20" FontStyle="Italic"/>

                <!-- SAYC -->
                <Border Background="White" Padding="15" Margin="0,5" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding SaycTitle}" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock Text="{Binding SaycContent}" FontSize="16" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Goren -->
                <Border Background="#EAEAEA" Padding="15" Margin="0,5" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding GorenTitle}" FontSize="20" FontWeight="Bold" Foreground="#555"/>
                        <TextBlock Text="{Binding GorenContent}" FontSize="16" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Acol -->
                <Border Background="#EAEAEA" Padding="15" Margin="0,5" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding AcolTitle}" FontSize="20" FontWeight="Bold" Foreground="#555"/>
                        <TextBlock Text="{Binding AcolContent}" FontSize="16" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- 2/1 -->
                <Border Background="#EAEAEA" Padding="15" Margin="0,5" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding TwoOverOneTitle}" FontSize="20" FontWeight="Bold" Foreground="#555"/>
                        <TextBlock Text="{Binding TwoOverOneContent}" FontSize="16" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Button Command="{Binding DataContext.NavigateHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                    Content="Back to Lobby" Margin="0,40,0,40" Padding="20,10" HorizontalAlignment="Left" FontSize="16"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
